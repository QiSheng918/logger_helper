# Abstract
easy wrapper spdlog  

# Dependency
```bash
sudo apt-get install libspdlog-dev
```
# Usage

* g++
```bash
g++ main.cpp -std=c++11 -l pthread
```
* CMake
```bash
mkdir build && cd build
cmake ..
make
```