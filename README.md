# Abstract
easy wrapper spdlog  

# Dependency
```bash
sudo chmod +x install.sh
sh ./install.sh
```
# Usage

* g++
```bash
g++ main.cpp -std=c++11 -l pthread
```
* CMake
```bash
mkdir build && cd build
cmake ..
make
```